# Copy to /opt/studyflow/.env and fill in. Never commit the real file.
ADMIN_EMAIL=daniel.marek@dmarkalogistika.cz

# Web push (daily reminders). Generate once with:
#   npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:daniel.marek@dmarkalogistika.cz
